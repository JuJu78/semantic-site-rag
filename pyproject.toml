[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "semantic-site-rag"
version = "0.1.0"
description = "Build a living Supabase RAG knowledge base for any website."
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}
